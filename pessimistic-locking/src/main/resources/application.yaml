spring:
  application:
    name: spring-pessimistic-postgresql

  datasource:
    url: jdbc:postgresql://localhost:5432/demo
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
